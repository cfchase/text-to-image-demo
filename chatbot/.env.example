# Root-level environment variables for Makefile
# Copy this file to .env and customize with your values

# Container Registry Configuration
REGISTRY=quay.io/cfchase
TAG=latest

# Local Development Ports
FRONTEND_PORT=8080
BACKEND_PORT=8000

# Build Configuration
BUILD_ARGS=--no-cache

# Kubernetes/OpenShift Deployment Configuration
NAMESPACE=chatbot                # Kubernetes namespace for deployment
OVERLAY=deploy                    # Kustomize overlay directory to use