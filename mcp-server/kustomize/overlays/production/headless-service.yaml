apiVersion: v1
kind: Service
metadata:
  name: mcp-server-headless
  labels:
    component: service-headless
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    component: server