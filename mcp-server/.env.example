# Example environment configuration for MCP Image Generation Server
# Copy this file to .env and adjust values as needed

# Service Configuration
SERVICE_NAME=mcp-image-generation-server
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8000
WORKERS=4

# Storage Configuration
# Options: file, s3
STORAGE_BACKEND=file
STORAGE_PATH=/tmp/mcp-images

# S3 Storage Configuration (if STORAGE_BACKEND=s3)
S3_BUCKET=my-image-bucket
S3_PREFIX=mcp-images/
S3_ENDPOINT_URL=https://s3.amazonaws.com
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1

# KServe Configuration
KSERVE_ENDPOINT=http://stable-diffusion-predictor.models.svc.cluster.local:8080
KSERVE_MODEL_NAME=stable-diffusion-v1-5
KSERVE_TIMEOUT=60.0
KSERVE_MAX_RETRIES=3

# Image Management
IMAGE_CLEANUP_INTERVAL=300  # 5 minutes
IMAGE_TTL=3600  # 1 hour
MAX_IMAGE_SIZE=10485760  # 10MB

# Development Settings
ENVIRONMENT=production  # Options: development, production