# Container runtime configuration
# Options: podman, docker
CONTAINER_RUNTIME=podman

# Container registry settings
IMAGE_REGISTRY=quay.io/cfchase
IMAGE_NAME=mcp-server
IMAGE_TAG=latest

# Port settings
PORT=8080

# Diffusers runtime URL (for local testing)
DIFFUSERS_RUNTIME_URL=http://0.0.0.0:8080

# Model ID for diffusers runtime
DIFFUSERS_MODEL_ID=segmind/tiny-sd

# Image output path
IMAGE_OUTPUT_PATH=/tmp/image-generator

# Image server URL (public URL for serving images)
IMAGE_SERVER_URL=http://localhost:8080/images